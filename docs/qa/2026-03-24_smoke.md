# Smoke Test 2026-03-24

## Scope

- Change: `phase1-single-workflow-pilot`
- Goal: 驗證第一次手動 workflow pilot 是否留下可 validate、可接手、可重播的證據鏈

## Commands

- `openspec change validate phase1-single-workflow-pilot --strict`
- `d:/program/copilot-workspace-template/.venv/Scripts/python.exe D:/program/copilot-workspace-template/bootstrap_copilot_workspace.py --verify-only --root D:/program/Personal-AI-Work-System`

## Checklist

- [x] OpenSpec change strict validate 通過
- [x] handoff 可對應當前任務狀態、下一步、驗證狀態、阻塞或無阻塞結果
- [x] roadmap / decision-log / runlog / QA / memory 已同步本次 pilot 結論
- [x] 下一位 agent 可只靠 repo 文件啟動下一次手動 workflow

## Results

- `openspec change validate phase1-single-workflow-pilot --strict`：PASS
- `bootstrap_copilot_workspace.py --verify-only --root D:/program/Personal-AI-Work-System`：PASS
- 非阻塞提醒：style guide 仍為 PENDING，需要未來提供 Stitch HTML 才能完成對應流程，但不影響本次 workflow pilot 驗收

## 流程驗證紀錄

### 實際有用的檔案

- `AGENTS.md`
- `docs/handoff/current-task.md`
- `docs/handoff/blockers.md`
- `docs/roadmap.md`
- `docs/agents/project-context.md`
- `docs/agents/commands.md`
- `openspec/config.yaml`

### 本次沒實際用到或資訊密度偏低的欄位

- `docs/handoff/current-task.md` 的 `Rollback / Recovery Notes` 在本次 docs-first pilot 幾乎沒有提供額外判斷價值
- `docs/handoff/current-task.md` 的 `Interfaces / Contracts Affected` 對非程式型 repo 較抽象，閱讀成本高於收益
- `docs/roadmap.md` 目前只有「目前狀態」區段，尚未覆蓋 `40-roadmap-governance.md` 建議的完整 milestones 與轉換記錄

### 摩擦點

- OpenSpec CLI help 不會直接揭露 `spec-driven` schema 的 artifact 格式，必須先建立 change 再透過 `openspec instructions` 查詢
- `openspec validate phase1-single-workflow-pilot --strict` 對 active change 名稱解析不穩定，改用 `openspec change validate ...` 才能直接驗證 change
- 首次 pilot 需要同時掃描 handoff、roadmap、project-context、commands，入口分散但可接受；仍需在 QA 明確指回哪些檔案最值得優先讀

### 最小修正建議

- 下一次 pilot 開始前，優先讀 `AGENTS.md`、handoff、roadmap、commands，不必一開始就展開全部 docs
- 若本 repo 持續使用 OpenSpec，應在 commands 或 repo memory 補一行：`spec-driven` schema 需先用 `openspec instructions` 反查 artifacts
- 保留 `current-task.md` 骨架，但可在下次 change 只調整未使用欄位說明，不必重寫整份 handoff 模板