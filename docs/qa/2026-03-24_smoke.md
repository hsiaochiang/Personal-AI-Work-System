# Smoke Test 2026-03-24

## Scope

- Change: `phase1-single-workflow-pilot`
- Goal: 驗證第一次手動 workflow pilot 是否留下可 validate、可接手、可重播的證據鏈

## Commands

- `openspec change validate phase1-single-workflow-pilot --strict`
- `d:/program/copilot-workspace-template/.venv/Scripts/python.exe D:/program/copilot-workspace-template/bootstrap_copilot_workspace.py --verify-only --root D:/program/Personal-AI-Work-System`

## Checklist

- [x] OpenSpec change strict validate 通過
- [x] handoff 可對應當前任務狀態、下一步、驗證狀態、阻塞或無阻塞結果
- [x] roadmap / decision-log / runlog / QA / memory 已同步本次 pilot 結論
- [x] 下一位 agent 可只靠 repo 文件啟動下一次手動 workflow

## Results

- `openspec change validate phase1-single-workflow-pilot --strict`：PASS
- `bootstrap_copilot_workspace.py --verify-only --root D:/program/Personal-AI-Work-System`：PASS
- 非阻塞提醒：style guide 仍為 PENDING，需要未來提供 Stitch HTML 才能完成對應流程，但不影響本次 workflow pilot 驗收

## 流程驗證紀錄

### 實際有用的檔案

- `AGENTS.md`
- `docs/handoff/current-task.md`
- `docs/handoff/blockers.md`
- `docs/roadmap.md`
- `docs/agents/project-context.md`
- `docs/agents/commands.md`
- `openspec/config.yaml`

### 本次沒實際用到或資訊密度偏低的欄位

- `docs/handoff/current-task.md` 的 `Rollback / Recovery Notes` 在本次 docs-first pilot 幾乎沒有提供額外判斷價值
- `docs/handoff/current-task.md` 的 `Interfaces / Contracts Affected` 對非程式型 repo 較抽象，閱讀成本高於收益
- `docs/roadmap.md` 目前只有「目前狀態」區段，尚未覆蓋 `40-roadmap-governance.md` 建議的完整 milestones 與轉換記錄

### 摩擦點

- OpenSpec CLI help 不會直接揭露 `spec-driven` schema 的 artifact 格式，必須先建立 change 再透過 `openspec instructions` 查詢
- `openspec validate phase1-single-workflow-pilot --strict` 對 active change 名稱解析不穩定，改用 `openspec change validate ...` 才能直接驗證 change
- 首次 pilot 需要同時掃描 handoff、roadmap、project-context、commands，入口分散但可接受；仍需在 QA 明確指回哪些檔案最值得優先讀

### 最小修正建議

- 下一次 pilot 開始前，優先讀 `AGENTS.md`、handoff、roadmap、commands，不必一開始就展開全部 docs
- 若本 repo 持續使用 OpenSpec，應在 commands 或 repo memory 補一行：`spec-driven` schema 需先用 `openspec instructions` 反查 artifacts
- 保留 `current-task.md` 骨架，但可在下次 change 只調整未使用欄位說明，不必重寫整份 handoff 模板

---

# Smoke Test 2026-03-24（Pilot 2 比較驗證）

## Scope

- Change: `phase1-entrypoint-guidance-pilot`
- Goal: 驗證固定入口順序與 CLI 指引是否讓第 2 次手動 workflow pilot 比第 1 次更順，並留下可直接比較的摩擦證據

## Commands

- `openspec instructions proposal --change phase1-entrypoint-guidance-pilot`
- `openspec instructions specs --change phase1-entrypoint-guidance-pilot`
- `openspec instructions design --change phase1-entrypoint-guidance-pilot`
- `openspec instructions tasks --change phase1-entrypoint-guidance-pilot`
- `openspec change validate phase1-entrypoint-guidance-pilot --strict`
- `d:/program/copilot-workspace-template/.venv/Scripts/python.exe D:/program/copilot-workspace-template/bootstrap_copilot_workspace.py --verify-only --root D:/program/Personal-AI-Work-System`

## Checklist

- [x] 依固定入口順序啟動第 2 次 pilot，並記錄實際閱讀順序
- [x] 以 `openspec instructions` 直接取得 proposal、specs、design、tasks 的 artifact 格式
- [x] 以 `openspec change validate phase1-entrypoint-guidance-pilot --strict` 完成 strict validate
- [x] workspace `--verify-only` smoke 通過
- [x] QA 明確對照第 1 次 pilot 的 artifact 反查摩擦、validate 命令誤判與入口掃描範圍

## Results

- `openspec instructions proposal/specs/design/tasks --change phase1-entrypoint-guidance-pilot`：全部成功回傳 `spec-driven` schema 對應的 output path 與章節格式，未出現先走 help 再補救的情況
- `openspec change validate phase1-entrypoint-guidance-pilot --strict`：PASS；CLI 僅提示 `openspec change ...` 語法未來將淘汰，但本 repo 目前仍以此命令作為已驗證可用的 strict validate 入口
- `bootstrap_copilot_workspace.py --verify-only --root D:/program/Personal-AI-Work-System`：PASS
- 非阻塞提醒：style guide 仍為 PENDING，屬既有狀態，未影響本次 docs-first / workflow-first pilot 驗收

## 實際入口順序

1. `AGENTS.md`
2. `docs/handoff/current-task.md`
3. `docs/handoff/blockers.md`
4. `docs/roadmap.md`
5. `docs/agents/commands.md`
6. `docs/qa/2026-03-24_smoke.md`（作為第 1 次 pilot 基線）
7. `docs/agents/project-context.md`
8. `openspec/changes/phase1-entrypoint-guidance-pilot/` artifacts

前 1 到 5 步已足以判定目前狀態、下一步與主要驗證方式；第 6 到 8 步屬針對比較與驗證所做的定向補讀，不屬於超出預期的入口掃描。

## 與第 1 次 pilot 的直接比較

| 比較面向 | 第 1 次 pilot 基線 | 第 2 次 pilot 結果 | 判定 |
|---|---|---|---|
| artifact 反查摩擦 | 先發現一般 help 不會直接揭露 `spec-driven` artifact 格式，之後才補用 `openspec instructions` | 直接執行 `openspec instructions proposal/specs/design/tasks --change phase1-entrypoint-guidance-pilot`，未重演 help 試錯 | 已消除 |
| validate 命令誤判 | 曾記錄 `openspec validate <change> --strict` 解析不穩，需改用 `openspec change validate ...` | 一開始就使用 `openspec change validate phase1-entrypoint-guidance-pilot --strict`，未重演命令誤判 | 已消除 |
| 入口掃描範圍 | 啟動時仍需同時掃描 handoff、roadmap、project-context、commands 才逐步收斂入口順序 | 先固定走 `AGENTS.md` → handoff → roadmap → commands；其餘文件只在狀態已清楚後定向補讀 | 已收斂 |

## Verdict

- 第 2 次 pilot 比第 1 次更順。
- 具體改善成立：沒有重演第 1 次的兩個 CLI 摩擦，且啟動所需的入口掃描已收斂到既定順序內。
- 本次未達成的只有 style guide 仍為 PENDING，但那是既有非阻塞狀態，不屬於本次 change 的回歸或新摩擦。