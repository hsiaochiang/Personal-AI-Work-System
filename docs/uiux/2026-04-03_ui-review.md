# UI Review 2026-04-03

## Review Scope

- 驗收 `plain-text-adapter-refactor` 對 `/extract` 頁面的最小 UI 變更
- 檢查重點：
  - 使用者仍以既有純文字貼上路徑操作
  - 頁面文案已正確說明 `PlainTextAdapter` 正規化階段
  - 新增的 adapter script 載入不造成頁面靜態回歸

## Findings

- `/extract` 頁面標題下方說明文案已更新，明確描述「先由 PlainTextAdapter 正規化 → 再提取候選知識」
- Step 1 標題由「貼上對話文字」調整為「貼上純文字對話」，與本 change scope 一致，沒有引入多來源 UI 或新操作分支
- 頁面已載入 `/js/conversation-adapters.js`，且 targeted smoke 可證明 `GET /extract` 與 adapter script 靜態提供正常
- 本次 UI 變更僅屬文案與 script 載入層，未改動版面結構、按鈕流程或審核/寫回路徑

## Decision

- UI review PASS
- 允許進入後續 Review Gate 重判；本次 `/extract` UI 沒有形成新的可見回歸風險

## Evidence

- `web/public/extract.html`
- `web/public/js/conversation-adapters.js`
- `docs/qa/2026-04-03_plain-text-adapter-smoke.md`
- `tools/verify_plain_text_adapter.js`

---

## Addendum — chatgpt-adapter

### Review Scope

- 驗收 `chatgpt-adapter` 對 `/extract` 頁面的可見變更
- 檢查重點：
  - 新增的 JSON / TXT 上傳入口是否維持在既有卡片內
  - ChatGPT 文案是否有清楚說明支援範圍
  - 頁面仍保留 plain text fallback，不引入多來源 selector

### Findings

- `/extract` 頁首文案已更新為 ChatGPT transcript / JSON + plain text fallback 的混合入口，與本 change scope 對齊。
- Step 1 新增的「上傳 ChatGPT JSON / TXT」按鈕與狀態膠囊都留在原卡片內，沒有額外 modal、drawer 或分頁。
- textarea placeholder 已明確列出三種支援輸入：ChatGPT transcript、ChatGPT JSON、一般純文字 fallback。
- UI 保持既有主操作節奏：載入內容後仍由同一顆「提取候選知識」按鈕啟動流程。

### Decision

- UI review PASS
- 本次 UI 增量清楚、範圍受控，未擴大到 multi-source selector 或多 conversation picker

### Evidence

- `web/public/extract.html`
- `web/public/css/style.css`
- `web/public/js/extract.js`
- `docs/qa/2026-04-03_chatgpt-adapter-smoke.md`
- `tools/verify_chatgpt_adapter.js`

---

## Addendum — local-import-vscode-copilot

### Review Scope

- 驗收 `local-import-vscode-copilot` 對 `/extract` 頁面的可見變更
- 檢查重點：
  - Copilot local import 是否維持在既有 Step 1 卡片內
  - 路徑輸入、session 清單與狀態提示是否清楚且不壓過原本 textarea 主流程
  - ChatGPT / plain text 入口是否仍保留

### Findings

- `/extract` 在既有 Step 1 卡片內新增了一個獨立的 Copilot 本機匯入面板，沒有引入新頁面、drawer 或 modal。
- 路徑輸入與「刷新 Copilot Sessions」按鈕集中在同一列，對照 scope 可清楚對應「可覆寫路徑 + 刷新清單」的需求。
- session 清單卡片顯示標題、更新時間、model 與 preview，足以辨識最近對話，且選取態有清楚 highlight。
- 原本的 ChatGPT JSON / TXT 上傳列與 textarea 主輸入面仍在 Copilot 面板下方，保留既有 fallback 操作節奏。

### Decision

- UI review PASS
- 本次 UI 增量維持單頁、單卡、單主按鈕節奏，符合最小安全修改；未擴大成 multi-source selector

### Evidence

- `web/public/extract.html`
- `web/public/css/style.css`
- `web/public/js/extract.js`
- `docs/qa/2026-04-03_local-import-vscode-copilot-smoke.md`
- `tools/verify_local_import_vscode_copilot.js`

---

## Addendum — source-attribution-in-memory

### Review Scope

- 驗收 `source-attribution-in-memory` 對 `/memory` 的可見 UI 變更
- 檢查重點：
  - 來源 badge 是否只附著在有 metadata 的記憶條目
  - badge 是否維持既有 memory card 排版，不造成資訊擁擠
  - 既有 legacy 條目是否保持可讀，不被 badge 邏輯干擾

### Findings

- `/memory` 沿用既有卡片與群組結構，只在 item card 右上角新增小型來源 badge，沒有重排整個頁面或引入新篩選器。
- badge 共用既有 `.source-badge` 視覺基線，僅補 `ChatGPT`、`Copilot`、`Plain`、`Custom` 配色，因此與既有 decisions badge 語彙一致。
- 沒有來源 metadata 的 legacy 條目不顯示 badge，卡片文字仍維持原本層級與行距，不會因空 badge 產生視覺噪音。
- 本次 UI 變更限於 `/memory` 顯示層與 `/extract` 寫回格式，不擴大到 import selector 或候選列表 badge。 

### Decision

- UI review PASS
- 本次 UI 增量清楚且收斂，符合 V3 Change 5 的最小安全範圍

### Evidence

- `web/public/js/memory-source-utils.js`
- `web/public/js/memory.js`
- `web/public/css/style.css`
- `docs/qa/2026-04-03_source-attribution-in-memory-smoke.md`
- `tools/verify_source_attribution_in_memory.js`
